1.OpenFeign底层使用的是Ribbon.默认的时间是1秒,只要服务端响应超过一秒,就会直接报错.
2.设置等待时间
  直接在客户端工程yml文件中配置即可.
  ribbon:
    #建立客户端和服务端连接所等待的时间,单位毫秒
    ConnectTimeout: 5000
    #建立连接后从服务端读取到可用资源所需要的时间
    ReadTimeout:  5000
	
  这样设置时间之后,超过时间还是会报异常,在真正调用时还要注意.
  
  

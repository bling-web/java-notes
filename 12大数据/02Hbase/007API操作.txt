一.获取和加入数据.
   其实大体过程都一样.
   1.获取数据.
     获取table对象
     获取get对象,可以将获取的条件,比如说family,quanlify这些传入进去,以获得指定的数据.
     执行get方法,同时将get对象传进去
   2.加入数据,
     获取table对象
     获取put对象,同样将传入的数据,列名和对应的value传进去
     执行put方法,同时将put对象传进去	
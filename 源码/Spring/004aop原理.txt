总体原理:
     其实就是通过代理实现.
     在我们使用过程中会配置一个切面,和一个切点,切点中会配置一个路径,比如是* com.wind.Controller.*.*(..).那么在spring进行容器注入的时候,就会将该路径下(Controller)的代理对象注入到容器当中,也就是存储在singleonObject(单例对象存储空间)
  一个concurrentHashMap中,在我们注入对象的时候得到的就是代理对象,在执行代理对象的时候就可以做一些操作,前置处理,后置处理这些.